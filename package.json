{
  "name": "linkproof-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "鏈證 LinkProof — Progressive Web App. A faithful web port of the iOS / Android anti-fraud URL checker.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite build --mode preview && vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "typescript": "^5.5.4",
    "vite": "^5.4.8",
    "vitest": "^2.1.2"
  }
}
